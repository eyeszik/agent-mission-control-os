# Agent Mission Control - LangGraph Backend

This is the Python backend service for the Agent Mission Control OS. It provides the LangGraph workflow execution engine, API endpoints via FastAPI, and persistence mechanisms.
