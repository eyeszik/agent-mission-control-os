# Production Foundation DAG

```text
Provider verification
  -> auth contract
  -> Postgres schema/persistence
  -> secret/config contract
  -> deployment manifests
  -> publication/spend/analytics adapters
  -> CI production gates
  -> preview deployment proof
```
